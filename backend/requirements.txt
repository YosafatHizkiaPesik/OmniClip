# --- Web framework ---
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
python-dotenv>=1.0.0

# --- YouTube ---
# yt-dlp harus tetap mutakhir. YouTube rutin mematahkan versi lama: gejalanya
# adalah HTTP 403 di tengah unduhan atau "Sign in to confirm you're not a bot".
# Perbaikannya selalu: pip install -U yt-dlp
yt-dlp>=2026.8.19

# --- Transkripsi lokal (cadangan bila video tidak punya caption) ---
# Dijalankan int8 di CPU. Model 'base' butuh ~400 MB RAM, 'small' ~900 MB.
faster-whisper>=1.2.1

# --- Smart reframe: mengikuti wajah pembicara saat memotong 9:16 ---
# Versi di-pin: cv2.FaceDetectorYN stabil di 4.11 dan wheel-nya teruji.
# Varian 'headless' dipakai karena server tidak pernah membuka jendela GUI —
# paket biasa menarik GTK/Qt yang tidak ada gunanya di sini.
#
# Model YuNet (232 KB) harus ada di backend/models/. Bila tidak ada, render
# otomatis memakai mode bilah kabur, tanpa error:
#   curl -sL -o models/face_detection_yunet_2023mar.onnx \
#     https://github.com/opencv/opencv_zoo/raw/main/models/face_detection_yunet/face_detection_yunet_2023mar.onnx
#
# Pengenal wajah SFace (37 MB) menjawab pertanyaan yang tidak bisa dijawab
# posisi: SIAPA wajah ini. Tanpa berkas ini nomor orang jatuh ke tempat duduk,
# yang benar untuk bidikan lebar dan keliru begitu kamera berpindah ke close-up:
#   curl -sL -o models/face_recognition_sface_2021dec.onnx \
#     https://github.com/opencv/opencv_zoo/raw/main/models/face_recognition_sface/face_recognition_sface_2021dec.onnx
opencv-python-headless==4.11.0.86

# Pengenal suara untuk memisahkan narasumber. Menjalankan CAM++ (3D-Speaker)
# sebagai satu berkas ONNX 27 MB — tanpa torch, yang tidak akan muat di
# anggaran memori mesin target bersama Whisper.
#
# Modelnya diunduh sendiri saat pertama dipakai ke backend/models/campplus_sv.onnx.
# Bila unduhan gagal, pemisahan narasumber dilewati tanpa menjatuhkan pipeline.
onnxruntime>=1.17,<2

# --- Hitungan bingkai dan audio ---
# Selama ini numpy ikut masuk hanya sebagai dependensi opencv dan
# faster-whisper. Ia dipakai langsung di hampir seluruh kode analisis, jadi
# menggantungkannya pada paket lain berarti sebuah pembaruan di paket itu bisa
# mencabutnya tanpa satu baris pun berubah di sini.
numpy>=1.26,<3

# --- AI (opsional) ---
# Tanpa API key, pemilihan klip tetap berjalan memakai mesin heuristik lokal.
google-genai>=0.1.1

# --- Unggah ke YouTube dan Google Drive ---
# Keduanya sempat dihapus dari berkas ini bersama lapisan Drive palsu yang lama,
# lalu jalur unggah yang sungguhan ditulis tanpa mengembalikan deklarasinya.
# Akibatnya tidak pernah terlihat di sini — paketnya masih tertinggal di venv
# pengembangan — dan baru muncul saat aplikasi dibangun di mesin bersih:
# tombol "Unggah ke YouTube" gagal dengan ModuleNotFoundError pada siapa pun
# yang meng-clone repo ini.
#
# google-auth-httplib2 dan httplib2 ikut sebagai dependensi keduanya.
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# --- Font subtitle ---
# Font display dibundel di app/assets/fonts (semuanya SIL OFL) dan diteruskan ke
# libass lewat `fontsdir`. Tanpa itu libass jatuh ke DejaVu Sans lewat
# fontconfig — font teks badan, dan pada ukuran judul hasilnya terlihat seperti
# berkas subtitle, bukan konten.

# Prasyarat sistem (bukan paket pip): ffmpeg dan ffprobe harus ada di PATH.
#   Ubuntu/Debian: sudo apt install ffmpeg

# Pembacaan judul klip.
#
# piper-tts berjalan di mesin ini (butuh berkas suara 63 MB yang diunduh sekali
# dari dalam aplikasi). edge-tts memakai suara Microsoft lewat jaringan — itulah
# suara yang dikenal orang dari kebanyakan alat pembuat klip, tapi antarmukanya
# tidak resmi dan bisa berhenti bekerja sewaktu-waktu, jadi ia pelengkap, bukan
# pengganti.
piper-tts==1.8.0
edge-tts
